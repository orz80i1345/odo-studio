/**
 * BookingListPage.tsx — 預約管理
 * 骨架 placeholder：之後串 listBookings + updateBookingStatus，含狀態篩選
 */
export function BookingListPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold text-ink">預約管理</h1>
      <p className="mt-2 text-ink-3">之後串 listBookings + updateBookingStatus，含狀態篩選</p>
    </section>
  )
}
