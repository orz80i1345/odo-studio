/**
 * LoginPage.tsx — 後台登入
 * 骨架 placeholder：之後以 RHF + Zod 實作，成功後存 token 並導向儀表板
 */
export function LoginPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold text-ink">後台登入</h1>
      <p className="mt-2 text-ink-3">之後以 RHF + Zod 實作，成功後存 token 並導向儀表板</p>
    </section>
  )
}
